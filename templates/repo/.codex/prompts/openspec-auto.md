Route this task through the repository's automatic OpenSpec workflow.

Follow `AGENTS.md` and the project skill `openspec-auto`.
Do not ask the user to manually type OpenSpec commands.
If the task is read-only, answer normally.
